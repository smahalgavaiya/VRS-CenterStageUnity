using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// This provides a way to index Drives and link imported Drives to existing Drives 
public class DriveIndex : MonoBehaviour
{
    public Drive frontLeftWheel, backLeftWheel, frontRightWheel, backRightWheel, motor1, motor2, motor3, motor4; 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
